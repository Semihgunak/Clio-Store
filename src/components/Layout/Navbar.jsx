import "./Header.css";

const Navbar = () => {
  return (
    <nav className="navbar">
      <a href="#">Home</a>
      <a href="#">About</a>
      <a href="#">Products</a>
      <a href="#">Contact</a>
      <a href="#">Blogs</a>
    </nav>
  );
};

export default Navbar;
